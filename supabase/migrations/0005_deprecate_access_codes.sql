-- ─────────────────────────────────────────────────────────────
--  Вхід за кодом доступу згорнуто на користь Telegram
--
--  Стовпці access_code / code_attempts з 0003 більше не читає й не
--  пише жоден код: єдиний вхід у кабінет — бот, який упізнає людину
--  за верифікованим номером (див. src/app/api/telegram/webhook).
--
--  Стовпці НЕ видаляємо: у вже виданих кодах може лишатись історія
--  оплат, а drop column незворотний. Замість цього — позначка в самій
--  схемі, щоб наступний читач не шукав код, який видає ці коди.
--  Прибрати остаточно можна, коли підтвердиться, що дані не потрібні.
-- ─────────────────────────────────────────────────────────────

comment on column public.orders.access_code is
  'DEPRECATED (0005): вхід за кодом вимкнено, лишився вхід через Telegram. Не читається кодом.';

comment on column public.orders.code_attempts is
  'DEPRECATED (0005): лічильник спроб для вимкненого входу за кодом. Не читається кодом.';

-- Індекс обслуговував пошук за кодом при вході — пошуку більше немає,
-- а індекс лишається накладними витратами на кожному записі в orders.
drop index if exists public.orders_access_code_idx;
